# Shared secret — must match PADDLE_OCR_API_SECRET in the Worker env
PADDLE_OCR_API_SECRET=change-me-to-a-random-string

# Optional: limit max file size in MB (default 15)
PADDLE_OCR_MAX_MB=15

# Optional: PaddleOCR CLI args
# PADDLE_OCR_USE_ANGLE_CLS=true
# PADDLE_OCR_LANG=en
